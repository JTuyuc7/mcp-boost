[package]
name = "mcp-tests-native"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
napi = { version = "2", default-features = false, features = ["napi4"] }
napi-derive = "2"
regex = "1"
once_cell = "1"

[build-dependencies]
napi-build = "2"

